setenv PATH @@install_scripts@@:$PATH
if ($?PYTHONPATH) then 
    setenv PYTHONPATH @@install_purelib@@:$PYTHONPATH
else 
    setenv PYTHONPATH @@install_purelib@@
endif 
setenv PANDA_CONFIG_ROOT ~/.pathena
setenv PANDA_SYS @@install_dir@@
setenv PANDA_PYTHONPATH @@install_purelib@@
