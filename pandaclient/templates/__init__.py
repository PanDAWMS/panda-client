"""
pandaclient.templates
Each sub-module implements one workflow template and must expose a ``build(**kwargs)``
function that returns a :class:`~pandaclient.workflow_description.WorkflowDescription`.
"""
